# Student Management REST API

A lightweight backend API to manage student records with full **CRUD operations**.  
Built with **Java**, **Spring Boot**, **Spring Data JPA**, and **H2 in-memory database**. 

## Features
- Create, read, update, delete student records
- RESTful API endpoints
- Easy to extend for authentication or front-end integration
